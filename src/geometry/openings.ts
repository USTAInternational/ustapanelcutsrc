import type { CalculationSettings, Opening, PanelPiece, Point2D, Surface } from '../domain/types';
import { bounds, clipPolygonByHorizontalStrip, clipPolygonByVerticalStrip, pointInPolygon, polygonArea } from './core';
const openingPoly=(o:Opening):Point2D[]=>[{x:o.x,y:o.y},{x:o.x+o.width,y:o.y},{x:o.x+o.width,y:o.y+o.height},{x:o.x,y:o.y+o.height}];
export const openingIsValid=(o:Opening,s:Surface)=>openingPoly(o).every(p=>pointInPolygon(p,s.polygon))&&o.width>0&&o.height>0;
export function applyOpeningsToPanels(panels:PanelPiece[],openings:Opening[],settings:Pick<CalculationSettings,'subtractOpenings'>):PanelPiece[]{return panels.map(panel=>{const cuts=openings.filter(o=>o.surfaceId===panel.surfaceId).flatMap(o=>{const p=clipPolygonByHorizontalStrip(clipPolygonByVerticalStrip(panel.polygon,o.x,o.x+o.width),o.y,o.y+o.height);if(p.length<3||polygonArea(p)<=0)return[];const b=bounds(p);return[{openingId:o.id,polygon:p,localX:b.minX-panel.positionX,localY:b.minY-panel.positionY,width:b.maxX-b.minX,height:b.maxY-b.minY}]});const cutArea=cuts.reduce((s,c)=>s+polygonArea(c.polygon),0),visible=Math.max(0,panel.visibleArea-(settings.subtractOpenings?cutArea:0));return{...panel,cutouts:cuts,visibleArea:visible,wasteArea:Math.max(0,panel.blankArea-visible)}})}
